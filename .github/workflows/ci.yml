name: CI

on:
  push:
    branches: ["**"] # tag pushes are gated by release.yml, which runs the same checks
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: ./.github/workflows/checks.yml
